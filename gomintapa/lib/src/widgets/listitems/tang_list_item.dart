import 'package:flutter/material.dart';

class TangListItem extends StatelessWidget {
  const TangListItem({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
