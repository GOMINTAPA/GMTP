import 'package:flutter/material.dart';

class MakListItem extends StatelessWidget {
  const MakListItem({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
